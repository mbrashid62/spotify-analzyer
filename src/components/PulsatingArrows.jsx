import React from 'react';

import '../styles/PulsatingArrows.scss';

const PulsatingArrows = () => (
  <div className="pulsating-arrows-container">
    <span className="arrow" />
  </div>
);

PulsatingArrows.propTypes = {};

export default PulsatingArrows;
