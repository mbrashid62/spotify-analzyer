import React, {
  useState,
  useEffect,
} from 'react';
import PropTypes from 'prop-types';
import { connect } from 'react-redux';

const Dashboard = ({}) => {
  return (
    <div>
      <h1>Hello World</h1>
    </div>
  );
};

Dashboard.propTypes = {

};

export default connect(() => ({

}), {

})(Dashboard);
