import React, {
  useState,
  useEffect,
} from 'react';
import PropTypes from 'prop-types';

const Dashboard = (props) => {
  return (
    <div>
      <h1>Hello World</h1>
    </div>
  );
};

Dashboard.propTypes = {

};

export default Dashboard;
